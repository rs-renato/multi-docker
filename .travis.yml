sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t courses/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true courses/react-test npm test

after_success:
  - docker build -t course/multi-client ./client
  - docker build -t course/multi-nginx ./nginx
  - docker build -t course/multi-server ./server
  - docker build -t course/multi-worker ./worker

# log in docker cli
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
# push the images in docker hub
  - docker push course/multi-client
  - docker push course/multi-nginx
  - docker push course/multi-server
  - docker push course/multi-worker